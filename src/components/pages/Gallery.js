import React, {useEffect} from 'react'

export default function Gallery({setPage}) {
    useEffect(() => {
        setPage("Gallery")
    });

    return (
        <div>
            
        </div>
    )
}
